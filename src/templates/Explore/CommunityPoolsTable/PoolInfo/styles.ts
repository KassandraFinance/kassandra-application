import styled, { css } from 'styled-components'

export const PoolInfo = styled.div`
  ${() => css`
    #ImagePool {
      display: flex;
      flex-direction: row;
      align-item: center;
      gap: 1.6rem;
    }
  `}
`
