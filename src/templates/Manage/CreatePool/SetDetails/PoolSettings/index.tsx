import * as S from './styles'

interface IPoolSettingsProps {}

const PoolSettings = ({}: IPoolSettingsProps) => {
  return <S.PoolSettings>PoolSettings</S.PoolSettings>
}

export default PoolSettings
